#include "pathfinder.h"

void mx_back_path(t_main *stct, t_retpath *stack) {
    int a = stack->c;

    /*
    if (get_from_stack(stack) == stack->path[0]) {
        mx_print_path(stct, stack);
        return;
    }
     */
}

void mx_get_stack_index(t_retpath *stack) {
    return
}
