#include "Pathfinder.h"

int main(int argc, char* argv[]) {
    printf("argc = %d\n", argc);
    /*
    char *str = argv[1];
    char *a = mx_file_to_str(str);
    mx_printstr(a);
    */
    const char *arr[] = argv[1];
    for (int i = 0; i < arr[i]; i++)


    return 0;
}
